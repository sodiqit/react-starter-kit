import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import { App } from './components/App';

ReactDOM.render(
  <App userName="Daniil" lang="Full boilerplate for React App" />,
  document.querySelector('.root'),
);
